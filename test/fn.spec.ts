const assert = require('assert')
const expect = require('chai').expect

import { fn } from '../src/utils/fn'

describe('Fn (functions utils) suite', () => {

    it('should be equal to true', () => {
        expect(true).to.equal(true)
    })


})
